
tasks:
  - clean_case:
      purge_generated: True
      remove_zero: yes
      remove_mesh: yes
      preserve: [ "*.xml" ]

  - run_command:
      cmd_name: blockMesh

  - run_command:
      cmd_name: relaxationZoneLayout

