
tasks:
  - clean_case:
      purge_generated: True
      remove_zero: no
      remove_mesh: yes
      preserve: [ "*.xml" ]

  - run_command:
      cmd_name: blockMesh

  - run_command:
      cmd_name: simpleSolver

  - process_logs:
      log_file: simpleSolver.log

