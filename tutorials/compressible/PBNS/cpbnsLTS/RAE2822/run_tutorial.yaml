
tasks:
  - clean_case:
      purge_generated: True
      remove_zero: no
      remove_mesh: no
      preserve: [ "*.xml" ]

  - run_command:
      cmd_name: cpbnsLTS

  - run_command:
      cmd_name: Mach

  - process_logs:
      log_file: cpbnsLTS.log

